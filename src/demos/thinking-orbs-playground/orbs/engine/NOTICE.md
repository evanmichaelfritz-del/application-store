`thinking-orbs-engine.es.js` and `thinking-orbs-engine.cjs` are vendored
unmodified from `thinking-orbs@0.3.1` (MIT), Jakub Antalik.

https://github.com/Jakubantalik/thinking-orbs

The app paints `MODE_FRAMES` / `resolvePreset` with Skia. It does not
reimplement particle recipes.
