export const THINKING_ORBS_PLAYGROUND_AGENT_PROMPT = "# AGENT_PROMPT — Thinking Orbs DISTINCT recreate (closed network)\n\nYou are implementing the focused Thinking Orbs demo in Palantir Code Workspace. Assume the network is unavailable. Do not browse, fetch, or consult any website. Do not tell the operator to browse libraries.dev, orbs.jakubantalik.com, X, or any other website. All requirements, copy, motion recipes, and install snippets needed for this task are below and in the local files named here.\n\n## Mission and scope\n\nBuild a dark-only, one-screen Thinking Orbs demo with exactly these surfaces:\n\n1. **Preview**: a static masonry/grid of fixed orb cards.\n2. **Playground**: a large stage, active orb, controls, and live code snippet.\n3. **Install & Usage**: React, React Native, and Swift UI sub-tabs with the locked snippets.\n\nThis is a focused Thinking Orbs demo, not a full libraries.dev home, shell, carousel, left rail, or the parked libraries.dev carousel Orbs visual. Do not add a libraries.dev home/rail, Get Pro, Star, unrelated cards, or extra routes. UI is dark only: no light theme UI and no Appearance toggle. A `dark` prop may appear in documentation copy, but it is not a chrome theme switch.\n\nRead local source files when needed:\n\n- `/workspace/recreate-intake/2084674723934318901/DESIGN_LOCK.md`\n- `/workspace/recreate-intake/2084674723934318901/MOTION_LOCK.md`\n- `/workspace/recreate-intake/2084674723934318901/PLATFORM_LOCK.md`\n- `/workspace/recreate-intake/2084674723934318901/BEHAVIOR_BRIEF.md`\n- `/workspace/parity-orbs/expo-072fd18/README.md`\n\nThe disk source of truth is `/workspace/parity-orbs/expo-072fd18`. The project root is the folder containing `app.json`. The target tree SHA is `072fd1892339443195e6ce755f4172a6e90ac9c5`.\n\n## Stack and app plumbing\n\nUse Expo + TypeScript + React Native Web, Shopify `@shopify/react-native-skia`, and Reanimated. `thinking-orbs-native` is not on npm; implement the local `ThinkingOrb` with Skia + Reanimated and keep the Install tab's upstream note verbatim. Use the vendored `thinking-orbs@0.3.1` engine files and its `MODE_FRAMES` / `resolvePreset` behavior rather than inventing particles or replacing the orb with the unrelated metallic carousel visual.\n\nThe web entry must load Skia once at the root:\n\n```ts\nLoadSkiaWeb({ locateFile: (f) => `/${f}` }).then(() => renderRootComponent(App))\n```\n\nShip `public/canvaskit.wasm` at the site root. Never call `LoadSkiaWeb` or `WithSkiaWeb` once per orb. Keep Preview canvases mounted with stable keys; use opacity/pointer handling or mount-once behavior, not focus/remount thrash. The Playground should mutate state and size props without remounting its Canvas wrapper. Use one shared clock for all orbs and pause offscreen work if necessary.\n\nOn Skia web, never dispose a `Picture` while any Canvas might still draw it. Do not use a one-rAF or short ring-of-24 disposal delay. Hold Pictures until unmount, then delay disposal a few frames. Paints and paths may be reused and disposed after use when not handed to a `<Picture />`.\n\n## Locked visual chrome\n\nCanvas is near-black (`#0a0a0a` class). Cards and panels are solid dark; type is white/mid-gray. Use soft full-pill state/size chips and approximately 12–16px stage/panel radii. Header copy:\n\n- Title: **Thinking orbs**\n- Subtitle: **Thought-orb loading indicators for AI interfaces, with nine hand-tuned animated states.**\n- Tabs: **Preview** and **Install & Usage**\n- Tab actions: **Tune in Studio** and **Copy prompt** as buttons only; do not build Studio.\n\n### Preview\n\nUse fixed cards in a dark masonry/grid. Each card has one orb and one presentation label; do not morph between states inside a card. Use these labels verbatim:\n\n- `Solving....`\n- `Thinking....`\n- `Agent listening...`\n- `Working....`\n- `Searching....`\n- `Solving....` (second card)\n- `Agent planning...`\n- `Agent thinking...`\n- `Agent shaping...`\n\nLarge cards are about size 64. Small cards are about size 20 and use the `Agent …` prefix where shown. Preserve four dots on large labels and three dots on Agent labels. On small Agent pills, render `Agent` muted gray and the action word bright white.\n\nPreview presentation labels are not playground state names. The only authorized direct presentation-to-prop wires are `Solving....` → `solving`, `Working....` → `working`, `Searching....` → `searching`, and `Agent listening...` → `listening`. Do not invent wires for Thinking, Planning, Agent thinking, or Agent planning. Those use the Motionsource table below. Agent shaping uses `msAgentShaping` and loops; it must not freeze on a triangle.\n\n### Playground\n\nLayout: section title **Playground**, large dark stage with centered orb, right control panel, and a live code snippet below the stage reflecting `state` and `size`.\n\nFree selectable state chips:\n\n- `Working`\n- `Searching`\n- `Solving`\n- `Listening`\n- `Connecting`\n- `Composing`\n- `Breathing`\n\nVisible but dimmed and disabled Pro chips: `Weaving`, `Shaping`, with gate copy **Available with Pro plan**. Free sizes are **64px** (default/chat-avatar) and **20px** (inline). Visible but disabled Pro size is **32px**, with the same gate chrome. Do not unlock 32, Weaving, or Shaping.\n\nCursor gravity controls are `Off` and `On`. When On, show exactly: **Draws the macOS pointer, so it only shows on a Mac.** Cursor gravity is not a Thinking Orbs package prop. On Linux/web intake there is no visible attraction or required pointer drawing.\n\nThe documented props are:\n\n```ts\nstate: \"working\" | \"searching\" | \"solving\" | \"listening\" | \"connecting\" | \"weaving\" | \"composing\" | \"breathing\" | \"shaping\"\nsize: 64 | 20\nspeed: number // default 1\ndark: boolean\npaused: boolean\n```\n\nThe 32px option is chrome-only until explicitly unlocked. Do not add state names or map the presentation words Thinking/Planning to `composing`, `weaving`, `breathing`, `connecting`, or any other prop.\n\n### Install & Usage\n\nProvide React, React Native, and Swift UI sub-tabs, copy affordances on code blocks, and these snippets verbatim.\n\n**React**\n\n```text\nnpm install thinking-orbs\nimport { ThinkingOrb } from 'thinking-orbs';\n<ThinkingOrb state=\"searching\" size={64} />\n```\n\n**React Native**\n\n```text\nnpm install thinking-orbs-native @shopify/react-native-skia react-native-reanimated\n```\n\nNote (verbatim): *thinking-orbs-native is not on npm yet — it lives in this repo at packages/thinking-orbs/ports/react-native/thinking-orbs-native. Expo needs expo run:ios / run:android (native modules).*\n\n```text\nimport { ThinkingOrb } from 'thinking-orbs-native';\n<ThinkingOrb state=\"searching\" size={64} />\n```\n\n**Swift UI**\n\n```text\n.package(path: \"packages/thinking-orbs/ports/ios/ThinkingOrbsKit\")\nimport ThinkingOrbsKit\nThinkingOrb(state: .searching, size: .px64)\n```\n\n## Motion authority\n\nUse the vendored `thinking-orbs@0.3.1` `MODE_FRAMES`, `STATE_TO_MODE`, and `resolvePreset` as the timing and geometry authority. Paint `OrbFrame` dots/lines through Skia Circles/Lines. The nine engine states and recipes are:\n\n| state | mode | recipe |\n|---|---|---|\n| `working` | `orbits` | particles on tilted orbits |\n| `searching` | `globe` | scan-meridian sweeps a dotted globe |\n| `solving` | `rubik` | bands scramble, then click back solved |\n| `listening` | `wave` | waveform rolls through rings |\n| `connecting` | `web` | constellation wires itself |\n| `weaving` | `braid` | three strands plait; Pro |\n| `composing` | `ribbon` | undulating multi-band sash |\n| `breathing` | `ring` | slowly morphing ring, `faceOn` shared ribbon geometry |\n| `shaping` | `morph` | dotted circle → triangle → square; Pro |\n\nFree playground motion must match Working, Searching, Solving, Listening, Connecting, Composing, and Breathing. Weaving/Shaping recipes remain locked in chrome, though the Preview/marketing Motionsource can show Agent shaping.\n\n### Concrete `resolvePreset` values\n\nThese are separate 64 and 20 designs, not uniform scaling. Keep baked speed and options as specified:\n\n- **working/orbits** — 64: speed 1.885, orbitN 12, ghostN 40, ghostR 0.9, ghostA 0.5, particles 3, partR 1.2, partRDepth 1.6, rsPow 0.6, rMin 0.3. 20: speed 3.9, orbitN 3, ghostN 10, ghostR 2.16, particles 3, partR 2.88, partRDepth 3.84, rSizeMul 2.4.\n- **searching/globe** — 64: speed 2.015, latRings 11, lonDensity 29, rBase 0.69, rDepth 1.955, scanMul 4.08, dimBase 0.45, rSizeMul 1.15, inkFar 0.62, inkSpan 0.54. 20: speed 2.665, latRings 6, lonDensity 14, rBase 1.05, rDepth 2.975, scanMul 4.335, dimBase 0.45, rSizeMul 1.75.\n- **solving/rubik** — 64: speed 1.82, latRings 9, lonDensity 24, moveCount 14, rBase 0.63, rDepth 1.785, rActive 0.315, rSizeMul 1.05. 20: speed 1.95, latRings 4, lonDensity 12, moveCount 14, rBase 1.14, rDepth 3.23, rActive 0.57, rSizeMul 1.9. Use engine cadence `lt(s, moveCount, 0.42, 1.2)`.\n- **listening/wave** — 64: speed 4.388, rings 9, lonDensity 23, rBase 0.6, rDepth 1.7. 20: speed 3.998, rings 5, lonDensity 13, rBase 0.96, rDepth 2.72, rSizeMul 1.6. Use waveform `0.62*sin(s*2.1 - ring*0.52) + 0.38*sin(s*1.27 + ring*0.83)`.\n- **connecting/web** — 64: speed 3.315, nodeN 41, thr 0.72, signals 7, nodeR 1.33, nodeRDepth 1.71, lineW 0.8, rSizeMul 0.95. 20: speed 6.63, nodeN 8, thr 0.72, signals 1, nodeR 2.128, nodeRDepth 2.736, rSizeMul 1.52. Draw lines and dots; this is the only engine mode with lines.\n- **weaving/braid** — 64: speed 1.625, strandN 26, turns 3, ghostN 75, rBase 1.2, rDepth 1.8. 20: speed 2.75, strandN 6, turns 3, ghostN 17, rBase 1.632, rDepth 2.448, rSizeMul 1.36. Three strands use `e/3 * 2π`.\n- **composing/ribbon** — 64: speed 2.34, lanes 3, segs 44, ghostN 38, rBase 0.935, rDepth 1.445, rSizeMul 0.85, spin 0, bandMul 3.9, wobMul 1. 20: speed 3.12, lanes 2, segs 20, ghostN 8, rBase 1.180, rDepth 1.824, rSizeMul 1.073, bandMul 4.94, wobMul 1.\n- **breathing/ring** — 64: speed 3.24, lanes 3, segs 44, ghostN 0, faceOn 1, rBase 1.052, rDepth 1.625, rSizeMul 0.956, spin 0, bandMul 3.627, wobMul 0.368. 20: speed 3.78, lanes 2, segs 15, ghostN 0, faceOn 1, rBase 1.784, rDepth 2.757, rSizeMul 1.622, bandMul 3.968, wobMul 0.565.\n- **shaping/morph** — 64: speed 2.405, rDot 0.008295, iconD 0.702, rMin 0.25, rSizeMul 0.395, spread 1.45. 20: speed 2.08, rDot 0.021231, iconD 0.53, rSizeMul 1.011, spread 1.45.\n\nShaping's t-space cycle is hold `V = 1.4`, morph `ot = 0.9`, smoothstep `n²(3-2n)`, shapes `[circle, triangle, square]`, period per shape `Q = 2.3`, full loop `6.9` in t. At 64 and speed prop 1 this is approximately 0.58s hold + 0.37s morph per shape and a 2.87s full loop. Agent shaping is the triangle phase of this loop, not a permanent 3D cone.\n\n## Clock, accessibility, and transitions\n\nUse one shared wall clock for every orb:\n\n```text\nt = performance.now() / 1000 * bakedSpeed * speedProp\n```\n\nAll instances stay in phase; do not start per-instance elapsed clocks at mount. `speed` defaults to 1 and multiplies baked preset speed. `paused === true` freezes the current frame and keeps the last `t`; do not clear it and do not emulate pause with `speed={0}`. State/size changes swap `resolvePreset` and mode frame without resetting wall-clock phase. Hidden/offscreen tabs pause the loop and resume from wall-clock phase.\n\nFor reduced motion (`AccessibilityInfo` / `prefers-reduced-motion`), paint exactly one static frame at `t = 0.6`, with no RAF and no Reanimated loop; theme/ink still applies. Cap device pixel ratio at 2. No blur filter is required: use plain light dots on dark.\n\nPicker transitions are an instant prop swap by default; no crossfade is required. If a crossfade is used, it may be opacity-only and no longer than 120ms. Preview cards continuously loop their fixed state and never morph between states.\n\nOrbs are non-interactive indicators. No pan/drag on the orb, no hover-to-gesture invention, and touch must not scrub `t`. Cursor gravity is chrome only: Off is identity; On may draw a macOS pointer overlay if ever implemented, but must not add an unsourced spring force to dots.\n\n## Motionsource table (Preview/Marketing only)\n\nThese keys are presentation API, not playground `state` props. Use the key to choose the engine mode plus size-specific options, keep the shared wall clock, and loop continuously. Never pass `state=\"thinking\"`, `state=\"planning\"`, or a renamed equivalent.\n\n| presentation label | Motionsource key | size | paint recipe |\n|---|---|---:|---|\n| `Thinking....` | `msThinking` | 64 | `MODE_FRAMES.ring` + breathing @64: faceOn 1, ghostN 0, speed 3.24, lanes 3, segs 44, etc.; hollow tilted ring |\n| `Planning....` | `msPlanning` | 64 | `MODE_FRAMES.ribbon` + composing @64: speed 2.34, lanes 3, segs 44, ghostN 38, bandMul 3.9, etc.; vertical ribbon/bands |\n| `Agent thinking...` | `msAgentThinking` | 20 | `MODE_FRAMES.ring` + breathing @20: faceOn 1, ghostN 0, and the locked 20px options |\n| `Agent planning...` | `msAgentPlanning` | 20 | `MODE_FRAMES.ribbon` + composing @20 and the locked 20px options; not orbits |\n| `Agent shaping...` | `msAgentShaping` | 20 | `MODE_FRAMES.morph` + shaping @20; loop circle → triangle → square, no frame-time pin |\n\nThe locked wire pattern is conceptually:\n\n```tsx\n<ThinkingOrbMotionsource key=\"msThinking\" size={64} />\n// msThinking → mode='ring' + breathing opts; msPlanning → mode='ribbon' + composing opts — never pass state props\n```\n\nDo not use `Thinking → composing`, `Planning → weaving`, or `Agent thinking → breathing` as state-prop renames. Solving, Working, Searching, Listening, Connecting, Composing, and Breathing in the playground remain real documented state values.\n\n## Optional marketing strip\n\nThe optional wide-screen marketing strip is not the Preview grid. Preview remains static masonry. If implemented, use only a right-column upward dark-glass pill gallery with a fixed left title block. Translate upward continuously and linearly at about **0.11 viewport-heights/second** (roughly one viewport height per 9 seconds; about 120px/s at 1080px height). Large pills are approximately 64 on one track; small 20/Agent pills are on a staggered second track in the gaps, creating a zig-zag. Combined crossing cadence is about 0.6–0.7 pills/s and a row crosses a fixed line about every 3s. Use a soft top/bottom vertical gradient mask and a seamless modulo-height loop. Never ease, spring, snap, fling, or scroll-scrub orb time. Pills keep their own looping recipe while the strip translates. The strip may show Thinking, Planning, and Agent shaping only through the Motionsource keys above.\n\n## Acceptance checklist and forbidden work\n\nBefore handoff verify:\n\n- Preview, Playground, and Install & Usage are present; no libraries.dev home/rail.\n- UI is dark only; no light theme controls.\n- Free states are exactly Working, Searching, Solving, Listening, Connecting, Composing, Breathing.\n- Weaving, Shaping, and 32px are visible but Pro-disabled with **Available with Pro plan**.\n- Preview labels, sizes, ellipses, and Agent dual-tone treatment match the lock.\n- The Motionsource table and keys are implemented without state-prop renames.\n- All orbs use a shared wall clock; reduced motion is a static `t=0.6` frame.\n- Skia loads once, `public/canvaskit.wasm` exists, and no mid-flight web Picture disposal occurs.\n- Install snippets and the React Native note are verbatim.\n\nForbidden: inventing Thinking→state or Planning→state maps; adding shimmer; adding a light theme UI; unlocking Pro features; inventing state names; replacing this with the libraries.dev carousel Orbs; building full libraries.dev home/rail; building a new grok.me app; browsing or instructing anyone to browse external sites; adding DOM targets or unsourced pointer/particle physics; freezing Agent shaping on a single triangle; disposing Pictures during active Canvas drawing.\n";
